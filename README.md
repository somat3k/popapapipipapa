# popapapipipapa

## Streamlit dashboard

Run the mobile-inspired admin dashboard with:

```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
```

Login using **User ID** `1` and **Password** `qwerty` for the demo gate.
